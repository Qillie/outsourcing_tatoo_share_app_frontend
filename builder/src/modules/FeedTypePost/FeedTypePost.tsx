//* Import libraries
import React from "react"
import { StyleSheet, Text } from "react-native"

//* Import modules

import { Box } from "../../core/layout"

//* Import interfaces
import IFeedTypePost from "./interfaces/IFeedTypePost"

const FeedTypePost = (props: IFeedTypePost) => {
    //* States

    //* Functions

    const setStyle = () => {
        // let style:{ default: TextStyle } = { default: {} }
        
        // style.default.fontSize = setFontSize(props.variant)

        // return StyleSheet.create(style).default
    }

    //* Life cycles
    React.useEffect(() => {

    }, [])

    return (
        <Box>
            
        </Box>
    )
}

export default FeedTypePost